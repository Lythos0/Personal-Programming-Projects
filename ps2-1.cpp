#include <iostream>
#include <algorithim>

int main(){

char arr[] = {'a', 'b', 'c'};
do {
        cout << arr[0] << arr[1] << arr[2] << endl;
    } while (next_permutation(arr, arr + 3));

    return 0;
}

